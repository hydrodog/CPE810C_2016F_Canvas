#include "category.h"

Category::Category()
{
    Category()
    {

    }

    void setHomework(double homeworkgrade)//homeworkgrade is a function from data group
    {
        for..
        for...
        homework[i][j]=homeworkgradep[i][j];
    }

    void setTest(double testgrade)//testgrade is a function from data group
    {
        for..
        for...
        test[i][j]=testgradep[i][j];
    }

    void setProject(double projectgrade)//projectgrade is a function from data group
    {
        for...
        project[i]=projectgradd[i];
    }

    double getHomework()
    {
        for...
        for...
        return homework[i][j];
    }

    double getTest()
    {
        for...
        for...
        returnt test[i][j];
    }

    double getProject()
    {
        for...
        return project[i];
    }

    void arrangmentH_L()//arrage all scores from the highest to lowest or vice versa.
    {
        for...
            for...
           if(grade[i]<[i-j])
           {
               double temp= grade[i];
               grade[i]=grade[i-j];
               grade[i-j]=temp;
           }
    }
    void arrangmentL_H()
    {
        for...
            for...
           if(grade[i]>[i-j])
           {
               double temp= grade[i];
               grade[i]=grade[i-j];
               grade[i-j]=temp;
           }
    }
    double mean();
    {
        return blablabla...
    }
    double median();
    {
        return grade[20];
    }
    double variance();
    {
        //blablabla...
    }
}
