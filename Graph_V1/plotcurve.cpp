#include "plotcurve.h"

PlotCurve::PlotCurve()
{
    PlotCurve()
    {
        plotdata x(0,40),y(grade[0],grade[40])//plot the grade from assignment class
        plot(x,y);
    }
    ....
}
