#include "assignment.h"

assignment::assignment()
{
    assignment()
    {

    }

    void setGrade(double homeworkgrade) //set is a function of class data
    {
        for...
    }
    double getGrade()
    {
        for...
        return grade
    }
    void arrangmentH_L()//arrage all scores from the highest to lowest or vice versa.
    {
        for...
            for...
           if(grade[i]<[i-j])
           {
               double temp= grade[i];
               grade[i]=grade[i-j];
               grade[i-j]=temp;
           }
    }
    void arrangmentL_H()
    {
        for...
            for...
           if(grade[i]>[i-j])
           {
               double temp= grade[i];
               grade[i]=grade[i-j];
               grade[i-j]=temp;
           }
    }
    double mean();
    {
        return blablabla...
    }
    double median();
    {
        return grade[20];
    }
    double variance();
    {
        //blablabla...
    }
}
